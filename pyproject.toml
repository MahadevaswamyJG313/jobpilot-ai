[project]
name = "jobpilot-ai"
version = "0.1.0"
description = "AI-powered job discovery, matching, and application assistant."
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = []